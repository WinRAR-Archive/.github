# .github
WinRAR archive and WinRAR extract pack or unpack a named set with a visible list. WinRAR test checks the result before you drop sources or split volumes. Unpack into a new folder, keep sources on a failed read-through, name the container by set and date, and keep split parts together.
